프로그래밍 상에서의 클래스란 ??
객체가 가져야 하는 속성과 기능을 정의한 내용을 담고있는 설계 역할
프로그래밍 상에서의 객체란 ??
클래스가 정의 된 후 메모리상에서 할당되었을 때 이를 객체라고 함
프로그래밍 상에서의 인스터스란 ??
클래스를 기반으로 생성됨

class Person{
  string name = 'John';
  int age;
  string sex;
 }
 void main(){
     person p1 =new person();
     p1, age = 30;
  }
  
  class person{
     string name;
     int age;
     string sex;
 person({string name, int age, string sex}){
    this name = name;
    this age = age;
    this sex = sex;
   }
 }
 void main(){
    person p1 = new Person(age ; 30)
    person p2 = new Person();
    print(p1,age):
    print(p2,age):
 }
